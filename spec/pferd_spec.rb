# frozen_string_literal: true

RSpec.describe Pferd do
  it "has a version number" do
    expect(Pferd::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
